"""Career asset loading, normalization and registry.

This package turns private career documents (docx/md/txt) into normalized,
provider-neutral ``CareerAsset`` objects. It must never import a provider SDK.
"""
