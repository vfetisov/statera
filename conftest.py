"""Root conftest: makes the project root importable when running ``pytest``.

This is not a plugin and adds no fixtures; it only ensures ``app`` and other
top-level packages can be imported from the test suite.
"""
